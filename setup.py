from setuptools import setup, find_packages

setup(
    name="ai_search",
    version="1.0.0",
    description="AI Search for ERPNext",
    author="ajmalajz",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[]
)
